# Point Gradle at Java 21 (the toolchain target).
# Gradle 8.13's embedded Kotlin plugin fails to parse Java version strings
# above 21 (e.g. "25"), so the wrapper must be launched under Java 21.
# If you need to override the Java home, set the JAVA_HOME environment variable.
